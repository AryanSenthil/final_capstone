Run linters and code quality checks:

1. **Frontend**: Run ESLint if configured:
   ```bash
   cd /home/ari/Documents/final_capstone/frontend && npm run lint
   ```

2. **Backend**: Run Python linters if available (ruff, flake8, or pylint)

Report any linting errors or warnings that need attention.
