Run tests for the project:

1. **Backend tests**: Run pytest from the backend directory using the virtual environment:
   ```bash
   cd /home/ari/Documents/final_capstone/backend && .venv/bin/python -m pytest -v
   ```

2. **Frontend tests**: If tests exist, run them with npm:
   ```bash
   cd /home/ari/Documents/final_capstone/frontend && npm test
   ```

Report the results clearly, showing any failures with details.
